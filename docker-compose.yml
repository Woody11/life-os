services:
  life-os:
    build: .
    container_name: life-os
    restart: unless-stopped
    ports:
      - "3030:3030"
    volumes:
      - /mnt/user/appdata/life-os/data:/data
    env_file:
      - .env
